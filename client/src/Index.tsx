import * as ReactDOM from 'react-dom';
import Rotas from './Routes';

function render() {
    ReactDOM.render(
      <div>
        {/* <Rotas/> */}
        <h1>fsadadsa</h1>
      </div>, document.getElementById('root')!);
  }
  
  render();
  